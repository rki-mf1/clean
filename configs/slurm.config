workDir = params.workdir

executor {
    name = "slurm"
    queueSize = 100
}

process {
    withLabel: basics { cpus = 12 } 
    withLabel: minimap2 { cpus = 24 }
    withLabel: bbmap { cpus = 24 }
}